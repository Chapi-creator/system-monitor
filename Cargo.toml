[workspace]
resolver = "2"
members = ["src-tauri", "native"]

# El perfil release vive a nivel raíz del workspace y aplica a todos los
# miembros (exe Tauri, exe nativo y core).
[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
strip = true
panic = "abort"
